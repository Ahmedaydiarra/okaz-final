{% extends 'base.html.twig' %}

{% block title %} Contact{% endblock %}

{% block body %}
<div class="mb-auto container px-6 py-10 md:px-20 lg:px-40 md:py-20 mx-auto block">
{# <div  class=" container w-5/6 md:w-2/3 xl:w-1/2 mx-auto border-rounded px-6 py-10 md:p-10 lg:p-14 shadow-xl"> #}
    <div class="p-5 space-x-5 ">
    <h1 class="text-2xl md:text-3xl lg:text-4xl text-center uppercase text-darkblue font-semibold">Envoyer une demande</h1>
        <p class="text-center text-lg md:text-xl lg:text-2xl mt-10 gap-y-3">Vous avez une question? <br />Laissez-nous un message, nous nous ferons un plaisir de vous répondre.</p>


        <section id="adama" class="grid gap-y-6 px-2 py-8 md:p-10 lg:p-16">

            {# {{ form(formulaire) }} #}
            {# {{ form(formulaire) }} #}
            {{ form_start(formulaire) }}
            <div class="flex flex-col md:flex-row gap-4 md:gap-6 ">
            <div class="w-full md:w-1/2  ">
            {{ form_row(formulaire.nom) }}
            </div>
            <div class="w-full md:w-1/2  ">
            {{ form_row(formulaire.email) }}
            </div>
            </div>
            <div class="flex flex-col md:flex-row gap-4 md:gap-6 ">
            <div class="w-full md:w-1/2  ">
            {{ form_row(formulaire.sujet) }}
            </div>
            <div class="w-full md:w-1/2  ">
            {{ form_row(formulaire.message) }}
            </div>
            </div>
           <div class="flex justify-center mt-10">
			<button type="submit" class="text-sm lg:text-base shadow-md rounded-full px-7 py-2 lg:px-10 bg-orange hover-bg-darkgrey text-black font-semibold uppercase tracking-wider">Enregistrer</button>
			</div>

             {{ form_end(formulaire) }}

        </section>

        
        
    </div>
    {# </div> #}
</div>
{% endblock %}
